
---
group: Examples
title: ONS Surveys
layout: example/views/_master
---

{% from "components/access-code/_macro.njk" import onsUACInput %}
{% from "components/button/_macro.njk" import onsButton %}
{% from "components/panel/_macro.njk" import onsPanel %}
{% from "components/collapsible/_macro.njk" import onsCollapsible %}

{% set pageConfig = {
    "header": {
        "title": "ONS Surveys",
        "language": {
            "languages": [
                {
                    "url": "#0",
                    "ISOCode": "en",
                    "text": "English",
                    "current": true
                },
                {
                    "url": "#0",
                    "ISOCode": "ga",
                    "text": "Help",
                    "current": false
                },
                {
                    "url": "#0",
                    "ISOCode": "ga",
                    "text": "My account",
                    "current": false
                },
                {
                    "url": "#0",
                    "ISOCode": "ga",
                    "text": "Sign out",
                    "current": false
                }
            ]
        }
    },

    "footer": {
        "OGLLink": {
            "pre": 'All content is available under the',
            "link": 'Open Government Licence v3.0',
            "url": 'https://www.nationalarchives.gov.uk/doc/open-government-licence/version/3/',
            "post": ', except where otherwise stated'
        }
    }
   
} %}

{% set form =  {
    "method": "GET",
    "attributes": {
        "name": "EnterUAC",
        "action": "#",
        "onsubmit": "return validateUAC()"
    }
} %}

{% block main %}
        {% call onsPanel({
        "type": "success",
        "classes": "ons-u-mb-m",
        "iconType": "check",
        "iconSize": "xl"
    }) %}
        <h1>Thank yyou for completing the ONS survey</h1>
        <p>Your answers have been submitted for participant first name</p>
        <p>Submitted on:</p>
        <p>Submission reference:</p>


    {% endcall %}

    <p>If another household member wants to start their questionnaire using the same device, you will need to click the ‘leave this page’ link below. They will then need to log in using their unique 16 character access code in the email or letter they were sent.

    </p>
    <br><br>

    {% call onsPanel({
        "type": "warn",
        "classes": "ons-u-mb-m"
    }) %}
        <p>Make sure you <a href="/prototypes/example/RHuac">leave this page</a> or close your browser if using a shared device.</p>
    {% endcall %}
{% endblock %}

{% block bodyEnd %}

{% endblock %}